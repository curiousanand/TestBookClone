import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'TestBook Clone - Online Learning Platform',
  description: 'Comprehensive online learning platform for competitive exam preparation with courses, tests, and live classes.',
  keywords: ['online learning', 'competitive exams', 'test preparation', 'courses', 'live classes'],
  authors: [{ name: 'TestBook Clone Team' }],
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className} suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}